NODE_ENV=development
HOST=0.0.0.0
PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/logichain
# Generer une valeur avec : node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
# Copier cette valeur ci-dessous avant de demarrer l API.
JWT_SECRET=
JWT_EXPIRES_IN=8h
